{
  "name": "pod-image-generator",
  "version": "1.0.0",
  "description": "Print-on-Demand image generation app: FLUX.1-schnell + RMBG-1.4 via Netlify Functions",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "install:functions": "npm install --prefix .finale"
  },
  "dependencies": {
    "@huggingface/inference": "^3.1.4",
    "sharp": "^0.33.5"
  }
}
